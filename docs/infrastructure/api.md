# infrastructure — api

_N/A — this domain owns no HTTP endpoints._

Middleware intercepts requests but doesn't define routes. The matcher determines which paths it sees; the middleware itself either redirects or passes through.
