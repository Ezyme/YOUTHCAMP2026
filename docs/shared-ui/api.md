# shared-ui — api

_N/A — this domain owns no HTTP endpoints._

`app/manifest.ts` is generated by Next.js as `/manifest.webmanifest` (a metadata route, not a custom API route). It returns a static `MetadataRoute.Manifest` with no logic.
